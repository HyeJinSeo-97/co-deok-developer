# 배포(빌드) 환경 설정
# 시크릿은 이 파일에 넣지 않는다 — Vercel 대시보드의 Environment Variables에 등록한다
# (이 파일은 커밋되므로 값이 그대로 저장소에 남는다)

# 배포 도메인. 콜백 URL 오리진을 고정한다
# 프록시/로드밸런서 뒤에서는 요청 오리진이 실제 주소와 달라질 수 있어 반드시 명시한다
APP_ORIGIN=https://co-deok-developer-six.vercel.app
